import React, { Component } from "react"
import "./feedback.css"

export default props => {


    return(
        <div id="feedback" className={props.classes} >
            {props.value}
        </div>
    )










}